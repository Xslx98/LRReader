<?xml version="1.0" encoding="utf-8"?><!--
  ~ Copyright 2016 Hippo Seven
  ~
  ~ Licensed under the Apache License, Version 2.0 (the "License");
  ~ you may not use this file except in compliance with the License.
  ~ You may obtain a copy of the License at
  ~
  ~     http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing, software
  ~ distributed under the License is distributed on an "AS IS" BASIS,
  ~ WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  ~ See the License for the specific language governing permissions and
  ~ limitations under the License.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- androidx.biometric merges USE_FINGERPRINT for its pre-28 fallback;
         minSdk 28 always uses BiometricPrompt, so strip the legacy permission. -->
    <uses-permission
        android:name="android.permission.USE_FINGERPRINT"
        tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- No CAMERA permission: the app only delegates to an external camera app via
         ACTION_IMAGE_CAPTURE. Declaring CAMERA would force a runtime grant and make
         the capture intent throw SecurityException until granted. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- LANraragi: Removed ACCESS_DOWNLOAD_MANAGER (EH system downloader) -->

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <!--  鼠标适配  -->
    <uses-feature
        android:name="android.hardware.type.pc"
        android:required="false" />
    <application
        android:name="com.hippo.ehviewer.EhApplication"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme"
        tools:ignore="UnusedAttribute">
        <!--        tools:targetApi="n">-->



        <meta-data
            android:name="android.max_aspect"
            android:value="2.1" />

        <activity
            android:name="com.hippo.ehviewer.ui.splash.SplashActivity"
            android:alwaysRetainTaskState="true"
            android:configChanges="orientation|keyboardHidden|screenSize|uiMode"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="fullSensor"
            android:theme="@style/SplashTheme"
            android:windowSoftInputMode="stateHidden|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- LAUNCHER -->
                <!--                <category android:name="android.intent.category.HOME" />-->
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.hippo.ehviewer.ui.MainActivity"
            android:configChanges="screenSize|uiMode"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.Main"
            android:windowSoftInputMode="stateHidden">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!--                <category android:name="android.intent.category.LAUNCHER"/>-->
            </intent-filter>

            <!-- Launcher shortcuts are registered dynamically (AppShortcuts) so
                 they exist on the .debug application id too (INF-10). -->

            <!-- LANraragi: Removed E-Hentai deep links and image search intent filters -->
        </activity>

        <activity
            android:name="com.hippo.ehviewer.ui.GalleryActivity"
            android:configChanges="screenSize|uiMode"
            android:exported="false"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.Gallery">

            <intent-filter>
                <action android:name="com.samsung.android.support.REMOTE_ACTION"/>
            </intent-filter>

            <meta-data
                android:name="com.samsung.android.support.REMOTE_ACTION"
                android:resource="@xml/s_pen_actions"/>
        </activity>

        <activity
            android:name="com.hippo.ehviewer.ui.SettingsActivity"
            android:configChanges="screenSize|uiMode"
            android:theme="@style/AppTheme.Settings" />

        <!-- LANraragi: Removed EH-only ExcludedLanguagesActivity -->

        <!-- LANraragi: Removed legacy DirPickerActivity (SAF-only now) -->

        <activity
            android:name="com.hippo.ehviewer.ui.LicenseActivity"
            android:configChanges="screenSize|uiMode"
            android:label="@string/license"
            android:theme="@style/AppTheme.Toolbar" />

        <activity
            android:name="com.hippo.ehviewer.ui.SetSecurityActivity"
            android:configChanges="screenSize|uiMode"
            android:label="@string/set_pattern_protection"
            android:theme="@style/AppTheme.Toolbar" />

        <!-- LANraragi: Removed EH-only UConfigActivity -->

        <!-- LANraragi: Removed EH-only HostsActivity -->

        <activity
            android:name="com.hippo.ehviewer.shortcuts.ShortcutsActivity"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />



        <!-- UCrop image cropper activity -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <service
            android:name="com.hippo.ehviewer.download.DownloadService"
            android:label="@string/download_service_label"
            android:foregroundServiceType="dataSync" />

        <provider
            android:name="com.hippo.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="com.hippo.content.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />

        </provider>

        <!-- Continue-reading home-screen widget (issue #9) -->
        <receiver
            android:name="com.hippo.ehviewer.appwidget.ContinueReadingWidgetProvider"
            android:exported="false"
            android:label="@string/shortcut_continue_reading">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/appwidget_continue_reading_info" />
        </receiver>

    </application>
</manifest>
