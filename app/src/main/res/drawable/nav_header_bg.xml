<?xml version="1.0" encoding="utf-8"?>
<!-- Drawer header background: subtle brand gradient. Fixed teal in every
     theme on purpose — the header is a branded surface, not a themed one. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:angle="315"
        android:startColor="@color/teal_500"
        android:endColor="@color/teal_800" />
</shape>
