New: read an archive while it is still downloading. The reader uses the pages the download has already saved and fetches only the missing ones, writing them into the download folder so no page is downloaded twice; pages are now downloaded in reading order. New: download folders are named by the archive title only, without the ID prefix; existing folders are renamed once on the first launch after updating.